services:
  redis_code_c:
    image: redis:7
    container_name: verification_code_cache
    ports:
      - "${REDIS_CODE_C_PORT}:${REDIS_CODE_C_PORT}"
    command: >
      redis-server --requirepass "${REDIS_PASSWORD_CODE_C}" --port ${REDIS_CODE_C_PORT}
    restart: always
    networks:
      - microservices_net_01

networks:
  microservices_net_01:
    external: true