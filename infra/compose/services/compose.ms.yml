services:
  auth-service:
    build:
      context: ../../../services/ms/auth_service/
      dockerfile: Dockerfile
      target: ${NODE_ENV}
      additional_contexts:
        packages: ../../../packages/
    container_name: ${AUTH_SERVICE_CONTAINER_NAME}
    ports:
      - "${AUTH_SERVICE_PORT}:${AUTH_SERVICE_PORT}"
    volumes:
      - ../../../services/ms/auth_service/src:/app/services/ms/auth_service/src
      - ../../../packages/shared/dist:/app/packages/shared/dist
    restart: always
    env_file:
      - ../../../.env
    networks:
      - microservices_net
      - data_net
    healthcheck:
      test: ["CMD", "curl", "-f", "http://auth-service:${AUTH_SERVICE_PORT}/health"] # Replace with your actual health check command
      interval: 30s
      timeout: 10s
      retries: 3

  user-service:
    build:
      context: ../../../services/ms/user_service/
      additional_contexts:
        packages: ../../../packages/
      dockerfile: Dockerfile
      target: ${NODE_ENV}
    container_name: ${USER_SERVICE_CONTAINER_NAME}
    ports:
      - "${USER_SERVICE_PORT}:${USER_SERVICE_PORT}"
    volumes:
      - ../../../services/ms/user_service/src:/app/src
    restart: always
    env_file:
      - ../../../.env
    networks:
      - microservices_net
      - data_net
    healthcheck:
      test: ["CMD", "curl", "-f", "http://${user-service}:${USER_SERVICE_PORT}/health"] # Replace with your actual health check command
      interval: 30s
      timeout: 10s
      retries: 3


  auth-mailer-service:
    build:
      context: ../../../services/ms/auth_mailer_service/
      additional_contexts:
        packages: ../../../packages/
      dockerfile: Dockerfile
      target: ${NODE_ENV}
    container_name: ${AUTH_MAILER_SERVICE_CONTAINER_NAME}
    ports:
      - "${AUTH_MAILER_SERVICE_PORT}:${AUTH_MAILER_SERVICE_PORT}"
    volumes:
      - ../../../services/ms/auth_mailer_service/src:/app/src
    restart: always
    env_file:
      - ../../../.env
    networks:
      - microservices_net
      - data_net
    healthcheck:
      test: ["CMD", "curl", "-f", "http://auth-mailer-service:${AUTH_MAILER_SERVICE_PORT}/health"] # Replace with your actual health check command
      interval: 30s
      timeout: 10s
      retries: 3
  # captcha_verification_service:
  #   build:
  #     context: ../src/ms/captcha_verification_service/
  #     additional_contexts:
  #       packages: ../../../packages/
  #     dockerfile: Dockerfile
  #     target: ${NODE_ENV}
  #     container_name: ${CAPTCHA_VERIFICATION_CONTAINER_NAME}
  #   ports:
  #     - "${CAPTCHA_VERIFICATION_PORT}:${CAPTCHA_VERIFICATION_PORT}"
  #   volumes:
  #     - ../src/ms/captcha_verification_service/src:/app/src
  #   restart: always
    # healthcheck:
    #   test: ["CMD", "curl", "-f", "http://localhost:8080/health"] # Replace with your actual health check command
    #   interval: 30s
    #   timeout: 10s
    #   retries: 3

networks:
  microservices_net:
    external: true

  data_net:
    external: true
